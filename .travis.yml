language: cpp
os: linux
dist: bionic
addons:
  apt:
    packages:
    - libfftw3-dev
    - libgtest-dev
    - openmpi-bin
    - libopenmpi-dev
    - libnetcdf-c++4-dev

before_install:
- cd /usr/src/gtest
- sudo cmake CMakeLists.txt
- sudo make
- sudo cp *.a /usr/lib

script:
- cd makefiles
- ./run_tests_ubuntu.sh
